get:
  tags:
    - Enrollment
  summary: Get all active enrollment links for given customer
  operationId: getEnrollmentLinksUsingGET
  parameters:
    - name: customerId
      in: query
      description: customerId
      required: true
      schema:
        type: string
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            $ref: ../components/schemas/EnrollmentLinks.yaml
